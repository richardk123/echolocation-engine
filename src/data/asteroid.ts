import {Line} from "./line";
import {Scene} from "./scene";

export class Asteroid
{
    private _lines: Line[];

    constructor(vertCount: number, scene: Scene)
    {
        for (let i = 0; i < vertCount; i++)
        {
            this._lines.push()
        }
    }
}